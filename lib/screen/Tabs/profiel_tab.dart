import 'package:flutter/material.dart';

class ProfielTab extends StatelessWidget {
  const ProfielTab({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Hello"),
    );
  }
}
